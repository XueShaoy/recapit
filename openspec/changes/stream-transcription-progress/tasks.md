## 1. 进度与估时模型

- [x] 1.1 定义与终端 UI 解耦的阶段和转写进度事件，包含总时长、最远处理位置、分段数、已耗时、可选 ETA 与可选文字，并通过单元测试验证字段和阶段转换
- [x] 1.2 实现进度百分比的边界限制和单调更新规则，通过乱序分段、超界时间、长静音及正常完成测试验证仅成功结束时达到 100%
- [x] 1.3 实现转写前耗时范围及运行中平滑 ETA 的纯计算逻辑，通过无历史、历史样本、样本不足、零进度和速度波动测试验证估算来源与降级行为

## 2. Whisper 流式进度

- [x] 2.1 重构 `FasterWhisperTranscriber`，在消费惰性分段生成器时逐段规范化并发送进度事件，同时保持最终分段排序和产物内容不变；通过生成器替身测试确认事件在生成器结束前到达
- [x] 2.2 在工作流中报告媒体校验、模型加载、转写、检查点写入和完成阶段，通过测试验证成功、空语音和模型异常路径的事件顺序
- [x] 2.3 增加无新分段期间的活动心跳快照，并通过可控时钟测试确认模型加载或静音期间仍持续更新已耗时且能可靠停止

## 3. 本机性能历史

- [x] 3.1 实现版本化的本机性能记录和运行配置键，只保存硬件/模型/设备/计算精度、音频时长、推理耗时与 RTF，并通过序列化测试确认不包含路径、文件名、哈希或转写内容
- [x] 3.2 实现平台缓存路径、近期样本限制、原子写入和中位数估算，通过缺失、损坏、不可读及不可写历史文件测试确认全部无损降级
- [x] 3.3 在成功转写后更新性能历史，在失败或中断时不写入成功样本，并通过工作流测试验证历史状态

## 4. CLI 进度体验

- [x] 4.1 在 `transcribe` 开始推理前输出录音时长、模型、设备、计算精度、预计耗时范围、估算来源及可能的首次下载提示，通过 CLI 测试验证中文信息完整
- [x] 4.2 实现交互终端单行动态显示，包含活动状态、百分比、音频位置、分段数、已耗时和 ETA，并通过模拟 TTY 测试验证刷新与最终换行
- [x] 4.3 实现非交互环境按最长 30 秒或每 5 个百分点输出状态行的节流策略，通过模拟时钟和捕获输出测试验证 Codex/日志环境不会长时间静默或过度刷屏
- [x] 4.4 增加 `--live-text/--no-live-text` 参数，默认不输出正文；启用后显示清理并截断的最新分段，通过 CLI 测试验证隐私默认值和显式开启行为
- [x] 4.5 完善成功、异常和键盘中断时的进度收尾，确认动态显示正常停止、错误信息单独成行且原有退出码和检查点策略不变

## 5. 文档与验收

- [x] 5.1 更新 README 和 `recording-recap` Skill，说明默认进度、耗时估算含义、首次下载边界和实时文字选项，并运行 Skill 校验器
- [x] 5.2 运行 Ruff、严格 mypy、pytest 和 `openspec validate stream-transcription-progress --strict`，确认现有转写、总结、时间码和产物测试没有回归
- [x] 5.3 使用现有长录音执行真实 `turbo/int8` 验收，记录首屏预估、模型加载心跳、至少三个中间进度点、动态 ETA、完成用时和非 TTY 输出，并确认最终转写内容与进度功能解耦
