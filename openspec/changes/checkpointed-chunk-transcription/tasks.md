## 1. 运行身份与配置模型

- [x] 1.1 增加源文件 SHA-256、内容短标识和安全 stem 生成逻辑，并用同名不同内容、同内容不同路径及短标识碰撞单元测试验证输出身份。
- [x] 1.2 增加 chunk 核心时长、上下文重叠、beam、VAD 和 hotwords 的类型化配置，默认设为 900 秒与 10 秒，并用默认值和非法边界配置测试验证。
- [x] 1.3 严格限制 `engine=faster-whisper` 并在媒体读取前拒绝未知引擎，以 CLI 配置错误测试验证不会调用 ffprobe 或模型工厂。
- [x] 1.4 增加规范化转写运行签名，明确纳入实际推理参数并排除 Markdown/Word 显示参数，以参数等价和差异矩阵测试验证签名稳定性。

## 2. Chunk 计划与本地音频提取

- [x] 2.1 实现覆盖完整源时长的确定性 ChunkPlan，包含核心和提取区间，并用 0、899、900、901、1800、3600 秒边界测试验证无空块、无核心空洞和正确上下文裁剪。
- [x] 2.2 实现单 chunk 的 ffmpeg 提取，将范围转换为单声道 16 kHz PCM 临时 WAV，并用命令参数测试及短音频集成测试验证时长和格式。
- [x] 2.3 为临时 chunk 建立成功、异常和 KeyboardInterrupt 清理路径，并通过故障注入测试验证同一时刻最多保留一个临时音频且退出后尽力清理。
- [x] 2.4 重构 faster-whisper 为一次运行复用一个模型的会话接口，并用模型工厂计数测试验证多 chunk 仅加载一次且分段仍逐段产生进度事件。

## 3. Chunk 检查点与 Manifest

- [x] 3.1 定义版本化 RunManifest、ChunkCheckpoint、chunk 状态和工作流阶段模型，并通过 JSON round-trip、未知版本和非法状态转换测试验证。
- [x] 3.2 实现 chunk 检查点的临时写入、fsync、原子替换、回读校验与摘要计算，并用每个写入故障点的测试验证半文件不会被提交。
- [x] 3.3 实现 manifest 的“数据文件先提交、manifest 最后提交”更新器，并通过 manifest 写入失败和磁盘文件被篡改测试验证消费者只信任匹配摘要的引用。
- [x] 3.4 增加同一录音运行的独占写锁与可诊断的遗留锁处理，并用双进程或锁替身测试验证并发写入被拒绝且不会静默删除活跃锁。
- [x] 3.5 实现恢复规划，只有源身份、运行签名、chunk 计划和检查点摘要全部匹配时才复用，并用缺失、损坏、参数变化和连续已完成 chunk 场景测试验证。

## 4. 全局合并、进度与性能历史

- [x] 4.1 将 chunk 相对分段转换为源录音绝对时间，并用首块、中间块、末块及源边界测试验证时间裁剪和偏移。
- [x] 4.2 实现按分段中点和核心区间的确定性归属、稳定排序与重叠去重，并用跨 15 分钟边界的合成语音分段测试验证最终文本只出现一次。
- [x] 4.3 增加相邻 chunk 的时间倒退、异常空洞和显著冲突检测，以可消解重复和必须失败的边界样例验证不会静默丢字。
- [x] 4.4 在全部 chunk 提交后构造现有 Transcription 并处理全程无语音情况，以 30、45、60 分钟模拟分段和空分段测试验证 schema、顺序与哈希。
- [x] 4.5 将总体进度聚合为已提交核心秒数加当前核心进度，并用恢复 50%、上下文不重复计数、单调性和最终 100% 测试验证。
- [x] 4.6 将性能历史 key 扩展为实际设备和推理参数签名，RTF 使用唯一源时长，并用 auto 解析、签名不匹配及历史回退测试验证估算来源。

## 5. 产物生命周期与 CLI

- [x] 5.1 调整输出路径为 `<stem>-<source-sha256前12位>` 并加入 `run.json`、`chunks/` 和 `recap.json`，以两个同名录音的 CLI 测试验证互不冲突。
- [x] 5.2 重构转写工作流为计划、顺序提取、逐块提交、合并和最终提交阶段，并用第三块中断后恢复测试验证前两块不重复推理。
- [x] 5.3 保持 `transcript.json` 为不可变转写检查点，将总结组合写入 `recap.json`，以渲染前后 transcript 字节比较和不同总结重复渲染测试验证。
- [x] 5.4 让 `recap.json`、Markdown 和可选 Word 按 manifest 记录部分成功状态，并通过 Markdown、manifest 和 Word 写入失败注入测试验证状态与退出码。
- [x] 5.5 为 `transcribe` 增加默认恢复和显式 `--restart`，移除其 `--overwrite` 语义，同时保持 `render --overwrite` 仅覆盖派生产物，并以 CLI 帮助和行为测试验证三种操作互不混淆。
- [x] 5.6 保持无 manifest 的旧版 `transcript.json` 可用于 `prepare-summary` 和 `render`，以现有 fixture 回归测试验证无需源音频即可生成 recap 和 Markdown。

## 6. 端到端验证与文档迁移

- [x] 6.1 更新 recording-recap Skill，使其识别兼容恢复、不可变 transcript、recap 最终路径和 `--restart` 授权边界，并通过契约文本测试验证不上传音频且仍只启动一个总结 Agent。
- [x] 6.2 更新 README 和配置示例，说明 15 分钟核心区间、10 秒上下文、恢复/重启/覆盖差异和新目录布局，并人工核对命令均可由 CLI help 对应。
- [x] 6.3 增加跨阶段端到端测试，覆盖全新运行、兼容恢复、参数不匹配、旧检查点渲染及可选 Word 部分失败，并运行 `ruff`、严格 `mypy` 和完整 `pytest` 验证。
- [ ] 6.4 使用具有跨边界语音的 30、45、60 分钟本地录音执行验收，记录峰值内存、总耗时、恢复耗时、重复/丢字检查和全局时间码结果，并验证音频及转写未发送到外部服务。
- [x] 6.5 运行 `openspec validate checkpointed-chunk-transcription --strict` 并把验收结论记录到 change 验证文档，确保所有 capability scenario 均有对应测试或人工证据。
